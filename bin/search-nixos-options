#!/usr/bin/env bash
set -eu

query="$1"

url="https://search.nixos.org/options?channel=unstable&from=0&size=50&sort=relevance&type=packages&query=${query}"

[ -n "$query" ] && $DEFAULT_BROWSER "$url"
